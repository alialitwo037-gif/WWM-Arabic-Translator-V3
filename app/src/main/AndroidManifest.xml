<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wwm.arabictranslator">

    <!-- الأذونات الأساسية للتطبيق والترجمة الشاشة -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="WWM Arabic Translator"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <!-- الشاشة الرئيسية (شاشة البداية) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- بقية شاشات التطبيق -->
        <activity android:name=".WwmAiActivity" android:exported="false" />
        <activity android:name=".GlossaryActivity" android:exported="false" />
        <activity android:name=".TranslationMemoryActivity" android:exported="false" />
        <activity android:name=".OverlaySettingsActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />

    </application>

</manifest>
